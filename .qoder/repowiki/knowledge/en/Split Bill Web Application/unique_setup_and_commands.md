- Development mode automatically deletes and recreates the `splitbill.db` SQLite database on startup to ensure schema consistency.
- CSS assets are generated by running `npm run build:css` (Tailwind CLI) which is hooked into the .NET build process via the `TailwindCompileBeforeBuild` target in `split_bill.csproj`.